
# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# Local HAR captures (large, may contain session IDs / analytics tokens) — never push.
# scripts/extract_saveas.py reads from and writes extracted bodies into this dir.
captures/
__pycache__/

# Beads issue tracker: local Dolt-backed working data (contains owner emails / machine paths)
.beads/
.pytest_cache/
