# Service code index

The SPA uses internal `serviceCode` strings that don't always match the public service name. This is the index from common references → SPA `serviceCode` → module file. Read the linked module before building any line item for that service.

## Currently supported

Most modules below reconcile to the cent — the formulas reproduce the captured `serviceCost.monthly` from a real saveAs body within rounding tolerance. Some do not: a few carry a documented residual (e.g. Route 53 ~$16, Systems Manager ~$30, VPC Site-to-Site VPN ~$20) or an unverified sub-path (e.g. ElastiCache Serverless, ~21% of that capture's total, is quarantined as best-effort). Each module's Verification section states its own residuals and what is round-tripped versus inferred — read it before quoting.

### Compute & databases

| User says | SPA serviceCode | Module |
|---|---|---|
| EC2, Linux/Windows VM, instance | `ec2Enhancement` | [`service-modules/ec2.md`](service-modules/ec2.md) |
| RDS MariaDB, MariaDB database, MariaDB read replica — **On-Demand + gp3 recompute-verified** (Single-AZ and Multi-AZ, eu-central-1, 2026-09-24); one line per instance, add each read replica as its own line; set `createRDSProxy`/`DatabaseInsightsSelected` to "0" (UI defaults are Yes); Reserved rows, gp3 above baseline and the add-ons are inferred | `amazonRDSMariaDB` (`estimateFor: rdsForMariaDB`) | [`service-modules/rds-mariadb.md`](service-modules/rds-mariadb.md) |
| RDS PostgreSQL, Postgres database — **`storageVolume` has no `"Aurora"` value** (the module used to list one; form 0.0.111 offers only gp2 / gp3 / io1 / io2 / Magnetic — Aurora is `amazonRDSAuroraPostgreSQLCompatibleDB`). Eight further form fields (`gp3Iops`, `gp3Throughput`, `provisioningIOPS`, `provisionedIOPSIO2`, `dedicatedLogVolume`, `numberOfHoursOnES`, `additionalBackupStorage`, `snapshotExport`) are documented as inferred-only | `amazonRDSPostgreSQLDB` | [`service-modules/rds-postgres.md`](service-modules/rds-postgres.md) |
| RDS SQL Server, MS SQL on RDS — **recompute-fragile**: SPA reuses the Oracle form (`estimateFor: "rdsForOracle"`) with a no-id `"undefined"` utilization column and deep dependent dropdown chains. Illegal/edge combos (BYOL+Web, BYOL+Express, a Reserved term for an edition/region with no RI SKU) make the recipient's "Update" fail. Validate the combo before emitting — see the module's "Recompute fragility" section. Form 0.0.134 note: **`optimize` is a CPU-configuration toggle** (its live labels are "Configure the number of vCPUs" / "Default CPU options"), not the Optimized Reads/Writes flag the module's prose calls it; `vcpuThread` / `vcpuCores` / `provisioningIOPS` / `provisionedIOPSIO2` / `additionalBackupStorage` exist in the form but are inferred-only | `amazonRDSForSQLServer` (`estimateFor` is the literal string `"rdsForOracle"`) | [`service-modules/rds-sqlserver.md`](service-modules/rds-sqlserver.md) |
| Fargate, ECS tasks, serverless containers, EKS pods on Fargate, Graviton/ARM Fargate — **Linux x86 and Linux ARM On-Demand verified** (ARM recompute-verified 2026-09-24 with `perMonth` tasks x `hr` duration); Windows, Fargate Spot, and ephemeral storage > 20 GB/task are NOT yet round-tripped | `awsFargate` (`estimateFor: template`) | [`service-modules/fargate.md`](service-modules/fargate.md) |
| EKS, Elastic Kubernetes Service, K8s control plane, EKS Hybrid Nodes, ArgoCD/ACK/KRO platform capabilities — covers control plane (Standard + Extended Support), Hybrid Nodes (per-vCPU tiered), and the EKS Capabilities add-ons; **EKS Auto Mode NOT captured**, **EC2 worker nodes** must be billed via the `ec2Enhancement` module, **Fargate pods on EKS** via `awsFargate` | `awsEks` (`estimateFor` is the literal `"Amazon EKS"` — with a space, not a form-id slug) | [`service-modules/eks.md`](service-modules/eks.md) |
| Aurora PostgreSQL, Aurora Postgres, Aurora cluster — **not the same as RDS PostgreSQL** (cluster-volume storage, and I/O-Optimized swaps both the instance and storage SKUs). Verified for **provisioned Aurora Standard only** (On-Demand + the 3-field Reserved split `TermType`/`LeaseContractLength`/`PurchaseOption` — the OLD packed `Reserved-1yr-No-Upfront-Standard` string recomputes the RI line to ~15% of stored); **Serverless v2 / Limitless, Aurora Global Database, and the exact Standard I/O model are NOT round-tripped** — capture a HAR before quoting them | `amazonRDSAuroraPostgreSQLCompatibleDB` (`estimateFor: AuroraPostgreSQLCompatibleDB`) | [`service-modules/aurora-postgres.md`](service-modules/aurora-postgres.md) |
| DynamoDB, DDB, NoSQL table, DAX, DynamoDB Streams/backups/PITR/CDC/export/import — group with `dynamoDbOnDemand`, `amazonDynamoDbProvisionedThroughputCapacity`, `amazonDynamoDbDaxClusters`, etc. as sub-services. On-demand, DAX, backup/PITR, and export/import reconcile to the cent, storage bills flat with **no 25 GB free-tier subtraction**, and reserved capacity is 1yr-only; **a non-trivial provisioned peak/baseline split is NOT quotable without a HAR** (upfront and recurring size off different capacity bases) | `amazonDynamoDb` (group; `estimateFor: simpleStorageServiceClassesGroup`) | [`service-modules/dynamodb.md`](service-modules/dynamodb.md) |
| ElastiCache, Redis (OSS), Valkey, Memcached, cache cluster, ElastiCache Serverless — node-based **Valkey On-Demand recompute-verified** (form 0.0.88, engine set per row via `Cache Engine`, 2026-09-24) and **Redis OSS On-Demand capture-verified**; **Serverless is best-effort** (the Serverless scalars are ~21% of the captured total and don't decompose into published ECPU/storage rates — keep them `"0"` unless the user asks for Serverless); only the Redis OSS `EngineType` token is captured (**Valkey / Memcache tokens and Reserved-node `TermType` strings are inferred** — form 0.0.87 gives Valkey `3N-kN93Pj_…` and gives **Memcache the SAME option id as Redis OSS**, so `EngineType` alone does not distinguish them); Redis Enterprise Cloud is Marketplace and NOT modeled | `amazonElastiCache` (`estimateFor: amazonElastiCache`) | [`service-modules/elasticache.md`](service-modules/elasticache.md) |

### Storage & networking

| User says | SPA serviceCode | Module |
|---|---|---|
| S3, S3 storage, S3 with data transfer — only `amazonS3Standard` + `awsS3DataTransfer` are documented; group form 0.0.85 offers seven further sub-services (`amazonS3StorageManagement`, `s3Vector`, `s3ObjectLambda`, `s3ExpressOneZone`, `s3AccessGrants`, `s3Files`, `s3Tables`) with **no field names or formulas in the module** — capture a HAR before emitting any of them | `amazonSimpleStorageServiceGroup` (group with `amazonS3Standard`, `awsS3DataTransfer`, etc. as sub-services) | [`service-modules/s3.md`](service-modules/s3.md) |
| VPC, Site-to-Site VPN, Transit Gateway, NAT Gateway (modelled as a Regional NAT GW x N AZs — the form requires the regional block; recompute-verified 2026-09-24), VPC data transfer (internet egress + cross-AZ, recompute-verified) | `amazonVirtualPrivateCloud` (group with `vpnConnectionVpc`, `transitGatewayVpc`, etc. as sub-services) | [`service-modules/vpc.md`](service-modules/vpc.md) |
| ELB, ALB, NLB, GWLB, Application/Network/Gateway Load Balancer (Classic LB NOT covered) — ALB LCU formula is the SPA's empirical bytes-additive variant, not AWS's documented max-across; tight quotes need a second capture | `elasticLoadBalancing` (group with `applicationLoadBalancer`, `networkLoadBalancer`, `gatewayLoadBalancer` sub-services) | [`service-modules/elb.md`](service-modules/elb.md) |
| CloudFront — **Flat-Rate Plans only** (Free / Pro / Business / Premium subscription tiers); usage-based per-GB / per-request CDN pricing is NOT yet supported — capture HAR and add module before quoting metered CDN traffic | `amazonCloudFront` (`estimateFor: productPackd1`) | [`service-modules/cloudfront.md`](service-modules/cloudfront.md) |
| FSx for Windows File Server — **`singleAZDeployment` only**; Multi-AZ Windows, Lustre, ONTAP, OpenZFS are NOT covered — capture and add a module before quoting them | `amazonFSx` (`estimateFor: singleAZDeployment`) | [`service-modules/fsx-windows.md`](service-modules/fsx-windows.md) |
| ECR, Elastic Container Registry, image registry, container image storage — storage + outbound DT only; replication / Enhanced scanning / pull-through cache are NOT modeled | `amazonElasticContainerRegistry` (`estimateFor: template_0`) | [`service-modules/ecr.md`](service-modules/ecr.md) |
| AWS Transfer Family — **Web Apps sub-service only**; SFTP/FTPS/FTP server endpoints and AS2/SFTP connectors are NOT yet captured. Don't quote anything but Web Apps without first capturing a HAR | `aWSTransferForSFTP` (lowercase `a`, uppercase `WS`; group with `webApps` sub-service) | [`service-modules/transfer-family.md`](service-modules/transfer-family.md) |
| AWS Managed Microsoft AD, Directory Service, Active Directory — **AWS Managed Microsoft AD only**; AD Connector and Simple AD have different `estimateFor` values and are NOT covered. Unusual shape: `columnFormIPM_*` arrays of row objects with human-readable string keys (`"Directory Size"`, `"Number Of Addl Domain Controllers"`, etc.) | `aWSDirectoryService` (lowercase `a`, uppercase `WS`; `estimateFor: managedMicrosoftActiveDirectory`) | [`service-modules/directory-service.md`](service-modules/directory-service.md) |
| Direct Connect, DX, dedicated network, private connection to AWS — **Dedicated ports captured**; hosted/sub-1G ports have the same shape but rates not yet verified. `region` field is decorative; pricing follows the `port:Direct Connect Location` colocation site | `awsDirectConnect` (`estimateFor: template`, flat shape with `columnFormIPM` row-objects + top-level `dataTransferOut` / `datatransferin` / `utilization`) | [`service-modules/direct-connect.md`](service-modules/direct-connect.md) |
| EBS, EBS volume, block storage, standalone gp3/io1 volume, EBS snapshot storage — the **dedicated** EBS line, distinct from EBS bundled in `ec2Enhancement` (prefer that module for storage attached to a fleet). Two distinct recompute-safe shapes: a gp3 volume line, and a **snapshot line that MUST use the gp2 `storageType`** + `snapshotFrequency: "1"` with `snapshotAmount ≈ 2×target-GB` (a gp3 snapshot line recomputes to $0.00). **Fast Snapshot Restore / restore volume is NOT modeled** (`monthly_fsr=0`) — refuse restore-heavy lines rather than quoting the ~$5 floor; **io2 / io2 Block Express NOT supported** | `amazonElasticBlockStore` (`estimateFor: elasticBlockStore`) | [`service-modules/ebs.md`](service-modules/ebs.md) |
| EFS, Elastic File System, NFS, shared file system — Regional (multi-AZ) with **Elastic Throughput verified**; `throughputModeSS` is an **opaque hashed token and only the Elastic value is captured** (Bursting / Provisioned tokens need a fresh HAR), One Zone and Provisioned-Throughput field names are inferred, and the exact split between the `Percentage_of_data` fields and the explicit `_Tiering`/`_Read` GB inputs is undocumented (~$0.30 residual on the reference capture) | `amazonEFS` (`estimateFor: elasticFileSystem`) | [`service-modules/efs.md`](service-modules/efs.md) |
| Route 53, DNS, hosted zones, DNS queries, health checks, Resolver endpoints, DNS Firewall, Traffic Flow — global service (most SKUs regionless; Resolver/Firewall SKUs are region-scoped). Shape and per-dimension rates are verified, but the reference capture leaves a **~$16/month residual unexplained** (`RRsetRecord`, `numberOfVPCs` associations, and a possible fast-interval per-check base are the suspects) and `numberOFhours` (ENI hours-per-month) is inferred — treat the aggregate as approximate | `amazonRoute53` (`estimateFor: Route53`) | [`service-modules/route53.md`](service-modules/route53.md) |

### AI / ML

| User says | SPA serviceCode | Module |
|---|---|---|
| Bedrock, Claude on Bedrock, LLM inference — **Anthropic In-Region On-Demand Standard only**; cross-region, Provisioned Throughput, Batch, and non-Anthropic providers (Nova/Cohere/Meta/AI21/Mistral/Stability) are NOT covered. The four opaque token fields (`modelSelectionIRstan` / `selectedModelIRstan` / `cacheReadIRstan` / `cacheWriteIRstan`) are now resolvable by model name from `data/anthropic/en_US.json` — no HAR needed for token selection, though a new model's math is still unvalidated. **Version anomaly: the `anthropic` pin was 0.0.37 while live serves 0.0.35** — corrected downward 2026-09-06; treat that capture's numbers with extra caution | `amazonBedrock` (`estimateFor: amazonBedrockClassesGroup`, group with `anthropic` sub-service) | [`service-modules/bedrock.md`](service-modules/bedrock.md) |

### Security & governance

| User says | SPA serviceCode | Module |
|---|---|---|
| Secrets Manager, secret storage | `awsSecretsManager` | [`service-modules/secrets-manager.md`](service-modules/secrets-manager.md) |
| Security Hub, compliance checks | `awsSecurityHub` | [`service-modules/security-hub.md`](service-modules/security-hub.md) |
| AWS Config, config items, conformance pack | `awsConfig` | [`service-modules/aws-config.md`](service-modules/aws-config.md) |
| CloudTrail, audit logs, CloudTrail Lake | `awsCloudTrail` | [`service-modules/cloudtrail.md`](service-modules/cloudtrail.md) |
| WAF, web ACL, web application firewall | `awsWebApplicationFirewall` | [`service-modules/waf.md`](service-modules/waf.md) |
| GuardDuty, threat detection — form 0.0.77 **adds `aiDataEvents`** (AI Protection, GB/month, region-gated); its Pricing API SKU is not yet resolved, so do not quote an AI Protection line without looking the rate up | `amazonGuardDuty` | [`service-modules/guardduty.md`](service-modules/guardduty.md) |
| Inspector, vulnerability scanning, SAST/SCA/IaC | `amazonInspector` (v2) | [`service-modules/inspector.md`](service-modules/inspector.md) |
| KMS, Key Management Service, customer-managed CMK, encryption key requests — **customer-managed CMKs + request volume only**; XKS / HMAC keys / CloudHSM custom key stores are NOT covered. `numberOfAsymmetricRsaRequests` is RSA-2048 only; RSA-3072/4096 + ECC sign/verify roll up under `numberOfAsymmetricRequestsExceptRsa2048` | `awsKeyManagementService` (`estimateFor: kms`, flat shape) | [`service-modules/kms.md`](service-modules/kms.md) |
| Systems Manager, SSM, Parameter Store, SSM Automation, Just-in-Time node access — **only those three sub-services covered**; Patch Manager / Run Command / OpsCenter / Change Manager / Incident Manager need a HAR before quoting. Parameter Store and Automation formulas are best-effort pending a second capture | `awsSystemsManager` (group with `awsSystemsManagerParameterStore`, `awsSystemsManagerAutomation`, `justInTimeNodeAccess` sub-services) | [`service-modules/systems-manager.md`](service-modules/systems-manager.md) |
| Shield, Shield Advanced, DDoS protection — **Shield Advanced only** (Standard is free, no line item); base $3,000/month subscription IS included in serviceCost; org-level subscription handling is NOT modeled — ask if user is in an AWS Organization | `awsShield` (`estimateFor: template`, flat shape; cc has top-level `cloudFrontUsage`, `LoadBalancingUsage` (PascalCase outlier), `elasticIpUsage`, `globalAcceleratorUsage` in tb\|month) | [`service-modules/shield.md`](service-modules/shield.md) |

### Streaming, messaging & queues

| User says | SPA serviceCode | Module |
|---|---|---|
| Kinesis Data Streams (Provisioned / OnDemand / OnDemand Advantage) | `amazonKinesisDataStreams` — `estimateFor` selects the mode | [`service-modules/kinesis-data-streams.md`](service-modules/kinesis-data-streams.md) |
| Data Firehose, Kinesis Firehose | `amazonKinesisFirehose` | [`service-modules/kinesis-firehose.md`](service-modules/kinesis-firehose.md) |
| SNS, topic, notification | `amazonSimpleNotificationService` (group with `standardTopics` + `fifoTopics` sub-services) | [`service-modules/sns.md`](service-modules/sns.md) |
| SQS, queue | `amazonSimpleQueueService` | [`service-modules/sqs.md`](service-modules/sqs.md) |
| Amazon MQ, ActiveMQ, RabbitMQ, message broker — 4 paths: RabbitMQ Cluster (Verified), ActiveMQ Single Instance + Active/Standby (Inferred), RabbitMQ Single Instance (Verified). **Form 0.0.60 REMOVED the `activeInstanceBroker` template** — Active/Standby is now `singleInstanceBroker` + `activeBrokerType: "0"` with `_2`-suffixed fields; and **ActiveMQ `instanceType` / `brokerStorageType` take RegionlessRateCode tokens, not the readable strings this module used to document**. Form 0.0.60 also adds `numberOfbrokersrunningdatareplication` on both templates and m7g RabbitMQ instance types the embedded token table does not cover (resolvable via `scripts/resolve_token.py mq`) | `amazonMQ` (multiple `estimateFor` values — see module's Coverage matrix) | [`service-modules/amazon-mq.md`](service-modules/amazon-mq.md) |

### Developer tools & CI/CD

| User says | SPA serviceCode | Module |
|---|---|---|
| CodeDeploy, on-prem deployments, hybrid CI/CD | `awsCodeDeploy` (on-prem path only; EC2/Lambda/ECS deployments are free) | [`service-modules/codedeploy.md`](service-modules/codedeploy.md) |
| CodePipeline, V1 active pipelines, V2 action-execution minutes | `awsCodePipeline` (combined V1 + V2 form; first V1 pipeline/account/month is free) | [`service-modules/codepipeline.md`](service-modules/codepipeline.md) |
| CodeBuild, managed build runners, CI build minutes | `awsCodeBuild` (**On-Demand EC2 fleet only**; Lambda / Windows / Reserved / GPU / macOS fleets are NOT yet captured — capture HAR and extend module before quoting them) | [`service-modules/codebuild.md`](service-modules/codebuild.md) |

### Monitoring, backup & disaster recovery

| User says | SPA serviceCode | Module |
|---|---|---|
| CloudWatch, metrics, alarms, dashboards, Logs, Logs Insights, Synthetics canaries, RUM, Contributor Insights, Database Insights — one flat line item covering every dimension. Per-dimension rates are individually verified but the **aggregate is NOT byte-perfect** against the capture: **mobile-RUM payload sizing and the Lambda-Insights cost path are SPA-internal (not Price-List-derived) and can't be reconciled to the cent**, and the $3.00/dashboard-month rate comes from the AWS pricing page with no Price-List API guardrail | `amazonCloudWatch` (`estimateFor: CloudWatch`) | [`service-modules/cloudwatch.md`](service-modules/cloudwatch.md) |
| AWS Backup, backup vault, centralized/cross-service backup — group with per-source sub-services (`amazonEfsBackup`, `ebsBackup`, `backupDynamoDb`, `rdsBackup`, etc.). **Percentage fields store the LITERAL percent** (`"10"` for 10%, not `"0.1"` — the fractional form collapses the cost on recompute); **not round-tripped end-to-end** — the seed `serviceCost.monthly` is approximated and the SPA recomputes it from the retention schedule; several sources expose only the LAGVault (cross-region copy) rate under `AWSBackup`, so warm/cold storage falls back to the source service's own snapshot SKU | `awsBackup` (group; `estimateFor: awsBackupSelector`) | [`service-modules/backup.md`](service-modules/backup.md) |
| DRS, Elastic Disaster Recovery, disaster recovery replication — group (`awsDRSGroups`) with `awsDrsRecoveryReplication` (all cost) + `awsDrsDrill` (`!HIDDEN` placeholder, $0). `subServices` **MUST be a JSON array, not an object** (an object makes the SPA fail to parse the ENTIRE estimate → every line item renders $0). The `auto`+`avg` EBS volume mode is **verified end-to-end** ($216.04 exact); **explicit gp3/gp2/st1 choices and the `min`/`max` cost types are NOT round-tripped**; replication-server EC2 hours and cross-region DT are excluded — add a separate EC2 line item | `awsElasticDisasterRecovery` (group; `estimateFor: awsDRSGroups`) | [`service-modules/drs.md`](service-modules/drs.md) |

## Not yet supported

**RDS for Oracle** — no module. Oracle shares the `rdsForOracle` form with RDS SQL Server but has its own `serviceCode`, license models (BYOL / LI), and editions (SE2/EE), none of which are captured. Refuse Oracle RDS line items and offer to capture a HAR to build the module — do not try to price Oracle through the SQL Server module.

The AWS Pricing Calculator covers 436 services. Coverage in this skill grows as modules are written. If the user's brief references a service not in the table above, follow the rule in `SKILL.md` (Step 1 / "When the user's brief covers a service you don't have a module for"): name the unsupported items, propose to skip or add a module, and let the user decide.

To add a new module, see `service-modules/_template.md` and the workflow in `SKILL.md`.
